# write a function for finding the average of two numbers
def avg(a,b):
    return (a + b) / 2

# there are two types of data to use in python:
# integers - whole numbers ... use function integer()
# floats - numbers with decimals ... use function float()

# to check a data type, simply use the function type()

# sometimes it is useful to create an empty list so you can add things later:
b = []

# in order to add things to a list, you have to append them
b.append(4)
b.append(5)
b.append(True)
b.append('hello')
